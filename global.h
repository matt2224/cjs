typedef enum {
    TYPE,
    ID,
    NUM,
    RETURN,
    EQUALS,
    STRING,
    SEMICOLON,
    COMMA,
    ENUM,
    PLUS,
    MINUS,
    L_BRACE,
    R_BRACE,
    L_BRACKET,
    R_BRACKET,
    L_PAREN,
    R_PAREN,
    EOF
} token;

